/**
 * @repo/tokens — design tokens via Style Dictionary
 *
 * Consume tokens as:
 * - CSS variables: import "@repo/tokens/tokens.css" then use var(--color-brand-primary), etc.
 * - Tailwind theme: import theme from "@repo/tokens/theme" and spread into theme.extend in tailwind.config
 */
export const tokensPackageVersion = "0.0.1";
